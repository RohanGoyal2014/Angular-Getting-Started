// Interfaces are only supported by TypeScript and can not be
// transpiled to JS, so they are development only
export interface IProduct {
    productId: number;
    productName: string;
    productCode: string;
    releaseDate: string;
    price: number;
    description: string;
    starRating: number;
    imageUrl: string;
}